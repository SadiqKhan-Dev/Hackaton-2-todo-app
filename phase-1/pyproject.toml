[project]
name = "todo-app"
version = "0.1.0"
description = "In-Memory Python Console Todo App"
readme = "README.md"
requires-python = ">=3.13"
authors = [
    {name = "Todo App Team"}
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = false
